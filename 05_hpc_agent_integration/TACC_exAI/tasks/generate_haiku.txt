generate code that copies and modifies all the needed code from below and then wraps it in a main function that does a structured generation call asking to generate a haiku about vines and then print the generated haiku from the structured generatiopn call to the console.  Also print out statements showing you're communicating with backend and awaiting response.  Assume we'll just be using a local ollama backend and don't include any of the openai compatible code.  Once you've generated this code, create a new knowledge base entry that contains a detailed README for the code with code snippets and syntax highlighting as appropriate.\n Starting Code: #paste#